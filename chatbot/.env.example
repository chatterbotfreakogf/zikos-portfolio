# Google Gemini API-Key (OpenAI-kompatibel via /v1beta/openai/) — optional.
# Ohne Key läuft der Bot mit dem Keyword-Fallback aus cv.json.
# Quelle: https://aistudio.google.com/apikey
GOOGLE_API_KEY=

# Nur für Production: damit der Service sich selbst pingt (Render Free).
RENDER_EXTERNAL_URL=
